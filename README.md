# Lake Zone Chemicals Limited CMS Website

Elegant static website and browser CMS for **LAKE ZONE CHEMICALS LIMITED**, a chemical supplier company in Tanzania.

## What is included

- Public company website: `index.html`
- Hidden CMS admin panel: `admin/`
- Editable company profile, products, industries, and insights
- Editable phone, email, WhatsApp number, location, map link, and SEO defaults
- Floating WhatsApp and call buttons
- Quote request form that opens the visitor's email app
- Export/import JSON content workflow
- SEO metadata, Open Graph tags, structured business schema, `robots.txt`, and `sitemap.xml`
- Extensionless public browsing through `/` and `/admin/`
- Responsive design for phone, tablet, and desktop screens

## How to open it

Open `index.html` in a browser to view the website.

Open `admin/` to edit content. Click **Save Changes** after editing. Content is saved in that browser. Use **Export JSON** and **Import JSON** to move CMS content between computers.

## GitHub usage

Clone or download this repository on another PC, then open `index.html`.

For public hosting, enable GitHub Pages for the repository branch and folder that contains these files.

If hosted on Apache or Netlify, the included `.htaccess` and `_redirects` files help redirect `.html` URLs to clean extensionless URLs.
